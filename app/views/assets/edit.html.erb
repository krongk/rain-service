<%= page_header(t('assets.edit.title'), {'资源列表' => new_asset_path, '控制面板' => '/'}) %>

<div class="row-fluid">
  <div class="span6">
    <%= render 'form', :asset_type => nil, :bucket => 'song-dev', :redirect_url => '/assets' %>
  </div>
  <div class="span6">
    <p>1. 在这里上传的资源，可以在任何地方引用。</p>
    <p>2. 视频推荐上传到优酷，然后引用代码到网站上; 上传到本地，视频解析的速度将很慢。</p>
  </div>
</div>