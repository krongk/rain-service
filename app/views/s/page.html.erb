<% title "#{@site_page.title} - #{@site.title}" %>
<% meta_keywords "#{@site.title}, #{@site_page.title}" %>
<% meta_description "#{@site.title}, #{@site_page.title}" %>

<%= raw @site_page.content %>
